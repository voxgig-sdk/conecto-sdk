# Conecto SDK

Conecto client, generated from the OpenAPI spec.

Customer-messaging platform API: live chat, AI agents, tickets and knowledge base.

Learn more about Conecto at [conecto.chat/developers](https://conecto.chat/developers).

This is an unofficial SDK for the Conecto public API, generated by Voxgig with [`@voxgig/sdkgen`](https://github.com/voxgig/sdkgen). It is not affiliated with, endorsed by, or sponsored by the upstream API provider.

Learn more about Voxgig SDKs at [voxgig.com/sdk](https://voxgig.com/sdk/).

> TypeScript, Python, PHP, Golang, Ruby, Lua, JavaScript SDKs, a CLI with an interactive REPL, and an MCP server for AI agents — all generated from one OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).

## Entities, not endpoints

This SDK exposes the API as **10 semantic entities** that you
call directly, instead of assembling URL paths and query strings. See the [Entities](#entities) table below for the full list. Entities are
**Capitalised** to mark them as the primary surface, each with the operations they
support (`list`, `load`, `create`, `update`, `remove`):

```ts
const client = new ConectoSDK()
const action = await client.Action().create({
  id: 'example',
  slug: 'example',
  ok: true,
})
```

Thinking in entities keeps the mental model small — for people and AI agents alike —
rather than reasoning about raw HTTP routes and query parameters.

## Offline unit testing

Every SDK ships a built-in **test mode** that swaps the HTTP transport for
an in-memory mock, so your unit tests run fully offline — no server, no
network, and no credentials:

### TypeScript

```ts
// The offline mock starts EMPTY — seed it with the records the test needs.
// Shape: { entity: { <entity-name>: { <id>: <record> } } }
const client = ConectoSDK.test({
  entity: {
    contact: {
      test01: { id: 'test01' },
    },
  },
})
const contacts = await client.Contact().list()
// contacts is an array of Contact entities, populated with mock data
// — call contacts[0].data() for the record itself
console.log(contacts)
```

### Python

```python
client = ConectoSDK.test()
contacts = client.Contact().list()
print(contacts)
```

### PHP

```php
// Seed fixture data so offline calls resolve without a live server.
$client = ConectoSDK::test([
    "entity" => ["contact" => ["test01" => []]],
]);
$contacts = $client->Contact()->list();
```

### Golang

```go
client := sdk.Test()
result, err := client.Contact(nil).List(
    nil, nil,
)
```

### Ruby

```ruby
# Seed fixture data so offline calls resolve without a live server.
client = ConectoSDK.test({
  "entity" => { "contact" => { "test01" => {} } },
})
contacts = client.Contact.list()
```

### Lua

```lua
local client = sdk.test()
local results, err = client:Contact():list()
```

### JavaScript

```js
const client = ConectoSDK.test()
const contacts = await client.Contact().list()
// contacts is an array of entities, populated with mock data
// — call contacts[0].data() for the record itself
console.log(contacts)
```

## Packages

| Language | Package | Install |
| --- | --- | --- |
| TypeScript | `@voxgig-sdk/conecto` | publish pending — [install from git tag](https://github.com/voxgig-sdk/conecto-sdk/releases) |
| Python | `voxgig-sdk-conecto` | publish pending — [install from git tag](https://github.com/voxgig-sdk/conecto-sdk/releases) |
| PHP | `voxgig-sdk/conecto` | publish pending — [install from git tag](https://github.com/voxgig-sdk/conecto-sdk/releases) |
| Golang | `github.com/voxgig-sdk/conecto-sdk/go` | `go get github.com/voxgig-sdk/conecto-sdk/go@latest` |
| Ruby | `voxgig-sdk-conecto` | publish pending — [install from git tag](https://github.com/voxgig-sdk/conecto-sdk/releases) |
| Lua | `voxgig-sdk-conecto` | publish pending — [install from git tag](https://github.com/voxgig-sdk/conecto-sdk/releases) |
| JavaScript | `@voxgig-sdk/conecto-js` | publish pending — [install from git tag](https://github.com/voxgig-sdk/conecto-sdk/releases) |
| Go CLI | `github.com/voxgig-sdk/conecto-sdk/go-cli` | `go install github.com/voxgig-sdk/conecto-sdk/go-cli/cmd/conecto@latest` |
| Go MCP server | `github.com/voxgig-sdk/conecto-sdk/go-mcp` | `go get github.com/voxgig-sdk/conecto-sdk/go-mcp@latest` |

## Quickstart

### TypeScript

```ts
import { ConectoSDK } from '@voxgig-sdk/conecto'

const client = new ConectoSDK({
  apikey: process.env.CONECTO_APIKEY,
})

```

See the [TypeScript README](ts/README.md) for the full guide.

## Surfaces

| Surface | Path |
| --- | --- |
| **SDK** (TypeScript, Python, PHP, Golang, Ruby, Lua, JavaScript) | `ts/` `py/` `php/` `go/` `rb/` `lua/` `js/` |
| **CLI** | `go-cli/` |
| **MCP server** | `go-mcp/` |

## Use it from an AI agent (MCP)

The generated MCP server exposes every operation in this SDK as an
[MCP](https://modelcontextprotocol.io) tool that Claude, Cursor or Cline
can call directly. Build and register it:

```bash
cd go-mcp && go build -o conecto-mcp .
```

Then add it to your agent's MCP config (Claude Desktop, Cursor, etc.):

```json
{
  "mcpServers": {
    "conecto": {
      "command": "/abs/path/to/conecto-mcp"
    }
  }
}
```

## Entities

The API exposes 10 entities:

| Entity | Description | API path |
| --- | --- | --- |
| **Action** | The Action entity (create). | `/integrations/{slug}/actions/{action}/run/` |
| **Contact** | The Contact entity (create, list). | `/contacts/` |
| **Conversation** | The Conversation entity (create, list, load, update). | `/conversations/` |
| **Credential** | The Credential entity (load). | `/me/` |
| **Integration** | The Integration entity (create, list, load). | `/integrations/` |
| **Media** | The Media entity (create). | `/media/` |
| **Message** | The Message entity (create). | `/widgets/{id}/visitors/{session}/message/` |
| **Schema** | The Schema entity (load). | `/schema/` |
| **Visitor** | The Visitor entity (create). | `/widgets/{id}/visitors/{session}/identify/` |
| **Webhook** | The Webhook entity (create, list, load, remove). | `/webhooks/` |

The operations available across these entities are **load**, **list**, **create**, **update**, **remove** — see each entity's
own list above for exactly which it supports.

## Quickstart in other languages

### Python

```python
import os
from conecto_sdk import ConectoSDK

client = ConectoSDK({
    "apikey": os.environ.get("CONECTO_APIKEY"),
})

```

### PHP

```php
<?php
require_once 'conecto_sdk.php';

$client = new ConectoSDK([
    "apikey" => getenv("CONECTO_APIKEY"),
]);

```

### Golang

```go
import sdk "github.com/voxgig-sdk/conecto-sdk/go"

client := sdk.NewConectoSDK(map[string]any{
    "apikey": os.Getenv("CONECTO_APIKEY"),
})

```

### Ruby

```ruby
require_relative "Conecto_sdk"

client = ConectoSDK.new({
  "apikey" => ENV["CONECTO_APIKEY"],
})

```

### Lua

```lua
local sdk = require("conecto_sdk")

local client = sdk.new({
  apikey = os.getenv("CONECTO_APIKEY"),
})

```

### JavaScript

```js
const { ConectoSDK } = require('@voxgig-sdk/conecto-js')

const client = new ConectoSDK({
  apikey: process.env.CONECTO_APIKEY,
})

```

## Direct and prepare

For endpoints the entity model doesn't cover, use the low-level methods:

- **`direct(fetchargs)`** — build and send an HTTP request in one step.
- **`prepare(fetchargs)`** — build the request without sending it.

Both accept a map with `path`, `method`, `params`, `query`,
`headers`, and `body`. See the [How-to guides](#how-to-guides) below.

## How-to guides

### Make a direct API call

When the entity interface does not cover an endpoint, use `direct`:

**TypeScript:**
```ts
const result = await client.direct({
  path: '/api/resource/{id}',
  method: 'GET',
  params: { id: 'example' },
})
if (result instanceof Error) {
  throw result
}
console.log(result.data)
```

**Python:**
```python
result = client.direct({
    "path": "/api/resource/{id}",
    "method": "GET",
    "params": {"id": "example"},
})
```

**PHP:**
```php
$result = $client->direct([
    "path" => "/api/resource/{id}",
    "method" => "GET",
    "params" => ["id" => "example"],
]);
```

**Go:**
```go
result, err := client.Direct(map[string]any{
    "path":   "/api/resource/{id}",
    "method": "GET",
    "params": map[string]any{"id": "example"},
})
if err != nil {
    panic(err)
}
fmt.Println(result)
```

**Ruby:**
```ruby
result = client.direct({
  "path" => "/api/resource/{id}",
  "method" => "GET",
  "params" => { "id" => "example" },
})
```

**Lua:**
```lua
local result, err = client:direct({
  path = "/api/resource/{id}",
  method = "GET",
  params = { id = "example" },
})
```

**JavaScript:**
```js
const result = await client.direct({
  path: '/api/resource/{id}',
  method: 'GET',
  params: { id: 'example' },
})
if (result instanceof Error) {
  throw result
}
console.log(result.data)
```

## Advanced

> Everyday use only needs the sections above. This explains the internals
> behind every call — relevant when writing custom features.

Every SDK call runs the same five-stage pipeline:

1. **Point** — resolve the API endpoint from the operation definition.
2. **Spec** — build the HTTP specification (URL, method, headers, body).
3. **Request** — send the HTTP request.
4. **Response** — receive and parse the response.
5. **Result** — extract the result data for the caller.

A feature hook fires at each stage (e.g. `PrePoint`, `PreSpec`,
`PreRequest`), so features can inspect or modify the pipeline without
forking the SDK.

### Features

| Feature | Purpose |
| --- | --- |
| **TestFeature** | In-memory mock transport for testing without a live server |

Pass custom features via the `extend` option at construction time.

## Per-language documentation

- [TypeScript](ts/README.md)
- [Python](py/README.md)
- [PHP](php/README.md)
- [Golang](go/README.md)
- [Ruby](rb/README.md)
- [Lua](lua/README.md)
- [JavaScript](js/README.md)

## Upstream API

This SDK is generated from the upstream OpenAPI specification. It is an
unofficial client and is not affiliated with the API provider.

The OpenAPI spec(s) this SDK was generated from are kept in the
[`.sdk/def/`](.sdk/def/) folder.

- Upstream API: [https://conecto.chat/api/v1](https://conecto.chat/api/v1)

## Security

Please report security issues to security@voxgig.com. See [SECURITY.md](SECURITY.md).
Do not open public issues for suspected vulnerabilities.

---

Generated from the Conecto OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).
